<Window x:Class="TD.WPF.Views.Trades.TradeImageDetailView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TD.WPF.Views.Trades"
        xmlns:behaviors="clr-namespace:TD.WPF.Behaviors"
        xmlns:i18n="clr-namespace:TD.i18n;assembly=OzzTradeDiary.i18n"
        Title="{x:Static i18n:LocalizedStrings.TradeImage}"
        Height="620" Width="720" MinHeight="400" MinWidth="480"
        WindowStartupLocation="CenterOwner"
        mc:Ignorable="d">
    <Grid Margin="8,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Image display -->
        <Border Grid.Row="0" BorderThickness="1" BorderBrush="#CCCCCC" Margin="0,0,0,8">
            <Image behaviors:AsyncImageLoader.SourceUrl="{Binding ImageURL, Mode=OneWay}"
                   Stretch="Uniform"
                   RenderOptions.BitmapScalingMode="HighQuality"/>
        </Border>

        <!-- Metadata -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Column="0" Grid.Row="0" FontWeight="Bold" Margin="0,0,6,4"
                       Text="{x:Static i18n:LocalizedStrings.Category}"/>
            <TextBlock Grid.Column="1" Grid.Row="0" Margin="0,0,12,4"
                       Text="{Binding Category, Mode=OneWay}"/>

            <TextBlock Grid.Column="2" Grid.Row="0" FontWeight="Bold" Margin="0,0,6,4"
                       Text="{x:Static i18n:LocalizedStrings.UpdatedAt}"/>
            <TextBlock Grid.Column="3" Grid.Row="0" Margin="0,0,0,4"
                       Text="{Binding UpdatedAt, StringFormat=\{0:yyyy-MM-dd HH:mm\}, Mode=OneWay}"/>

            <TextBlock Grid.Column="0" Grid.Row="1" FontWeight="Bold" Margin="0,0,6,4"
                       Text="{x:Static i18n:LocalizedStrings.Notes}"/>
            <TextBlock Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="3" TextWrapping="Wrap" Margin="0,0,0,4"
                       Text="{Binding Notes, Mode=OneWay}"/>

            <TextBlock Grid.Column="0" Grid.Row="2" FontWeight="Bold" Margin="0,0,6,4"
                       Text="{x:Static i18n:LocalizedStrings.ImageURL}"/>
            <TextBlock Grid.Column="1" Grid.Row="2" Grid.ColumnSpan="3" TextWrapping="Wrap"
                       Text="{Binding ImageURL, Mode=OneWay}"/>
        </Grid>
    </Grid>
</Window>
